export const SettlersGame = {
    setup: () => ({ tiles: Array(19).fill(null) }),

    moves: {
        // clickCell: ({ G, playerID }: { G: GameState, playerID: any }, id: any) => {
        //     G.tiles[id] = playerID;
        // },
    },
};
